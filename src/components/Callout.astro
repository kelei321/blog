---
interface Props {
  type?: "info" | "success" | "warning";
  title?: string;
}

const { type = "info", title = "提示" } = Astro.props;
---

<aside class:list={["callout", `callout-${type}`]}>
  <strong>{title}</strong>
  <div>
    <slot />
  </div>
</aside>
